<table>
	<% @students.each do |student| %>
		<tr>
			<td>
				<%= student.id %>
			</td>
			<td>
				<%= student.surname %>
			</td>
			<td>
				<%= student.name %>
			</td>
			<td>
				<%= student.email %>
			</td>
		
		</tr>
	<% end %>
</table>
<%#= @students.inspect %>